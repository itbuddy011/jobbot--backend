services:
  - type: web
    name: jobbot-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn core.main:create_app --factory --host 0.0.0.0 --port $PORT
